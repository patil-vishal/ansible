---
 - hosts: server1.infotech.com
   user: root
   vars :
    motd_welcome: 'welcome to linux\n'
   tasks: 
   - name: sample motd
     copy: 
      dest: /etc/motd
      content: "{{ motd_welcome }}"
